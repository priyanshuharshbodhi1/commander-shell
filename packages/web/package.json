{
  "name": "@devfleet/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "dev": "node dist/index.js",
    "test": "node --test --import tsx 'src/**/*.test.ts' || true",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@devfleet/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
